<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!--
	  Jailed iOS-friendly entitlements.
	  Do NOT add private entitlements here; they will not be granted on jailed devices.
	  Note: this project ships a single unsigned IPA intended to be re-signed by your
	  installer (SideStore/AltStore/StikDebug/TrollStore).
	-->
	<key>get-task-allow</key>
	<true/>
	<!--
	  Needed for TrollStore-based JIT workflows. JessiJITCheck.m checks this entitlement.
	  Note: Some signers/installers may strip or refuse this.

	  <key>dynamic-codesigning</key>
	  <true/>
	-->
	<!--
	  Allows loading third-party signed dylibs (e.g. downloaded JVM runtime) when supported
	  by the installer. Without this, dlopen() can fail due to library validation.
	-->
	<key>com.apple.security.cs.disable-library-validation</key>
	<true/>
	<!--
	<key>com.apple.developer.networking.multicast</key>
	<true/>
	-->
</dict>
</plist>
